# Python
.venv/
__pycache__/
*.pyc

# OS / editor
.DS_Store
.claude/settings.local.json

# Generated data — reproducible from config.yaml + seed
data/*
!data/.gitkeep

# Agent research cache (local notes, not project docs)
docs/.agent-cache/

# Tool caches
.ruff_cache/
.pytest_cache/

# dbt build output and installed packages (package-lock.yml is committed)
transform/target/
transform/dbt_packages/
transform/logs/
transform/.user.yml
